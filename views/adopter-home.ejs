<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
  </head>
  <body>
    <%- include('partials/navbar.ejs') %> <%- include('partials/hero.ejs') %>

    <div class="container mt-5">
      <h2 class="text-center mb-4 text-bg-dark position-relative">
        Pet Waiting To Long 😿
        <a
          href="/see-all"
          class="btn btn-outline-info"
          style="text-decoration: none"
          >See All</a
        >
      </h2>
      <div class="row">
        <% if (pets.length === 0) { %>
        <div class="col-12">
          <div class="empty-state">
            <h3>Belum Ada Pet</h3>
          </div>
        </div>
        <% } else { %> <% pets.forEach(pet => { %>
        <div class="col-md-4 mb-4">
          <div class="card h-100">
            <img
              src="<%= pet.image %>"
              class="card-img-top"
              alt="<%= pet.name %>"
              style="width: 100%; height: 200px; object-fit: cover"
            />
            <div class="card-body">
              <h5 class="card-title"><%= pet.name %></h5>
              <p class="card-text">
                <strong>Type:</strong> <%= pet.Type.name %><br />
                <strong>Breed:</strong> <%= pet.breed %><br />
                <strong>Price:</strong> Rp <%= pet.price.toLocaleString() %>
                <br />
                <strong>Waiting Time:</strong> <%= timeWaiting(pet.createdAt) %>
              </p>
              <div class="d-flex justify-content-between mt-3">
                <a
                  href="/pet/<%= pet.id %>"
                  class="btn btn-outline-primary btn-sm"
                >
                  See Detail
                </a>
              </div>
            </div>
          </div>
        </div>
        <% }); %> <% } %>
      </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
  </body>
</html>
